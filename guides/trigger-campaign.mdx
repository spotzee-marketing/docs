---
title: "Trigger a one-off campaign"
description: "Send a campaign on demand from your backend. The API equivalent of clicking Send in the UI."
keywords: ["campaigns", "trigger", "send", "blast", "transactional", "broadcast"]
---

Use this flow when you've already authored a campaign in the Spotzee UI (audience, template, send-time settings) and want to fire it on demand from your code. For example, the morning of a product launch or in response to a webhook from another system.

## What you'll build

A backend call that:

1. Looks up an existing campaign by ID.
2. Triggers a send for a single user, with an optional event payload that flows into the template.
3. Captures the response so you can correlate the send with downstream delivery events.

## Prerequisites

<Info>
  You'll need a `sk_` project key. The campaign must already exist in Spotzee. The API triggers existing campaigns rather than authoring new ones from scratch.
</Info>

## Walkthrough

<Steps>
  <Step title="Find the campaign ID">
    `GET /campaigns?limit=100` returns every campaign in the project with its `id`. In production, store the IDs you need in your config rather than looking them up on every send.

    A campaign in `state: "draft"` cannot be triggered. Promote it via the UI, or `PATCH /campaigns/{id}` to set `state: "scheduled"`.
  </Step>
  <Step title="Identify the user and event payload">
    The trigger endpoint fires a `trigger`-type campaign for **one user at a time**. The body has two required fields:

    | Field | Shape | Notes |
    |---|---|---|
    | `user` | object | Identifies the user. `external_id` is required; `email`, `phone`, `device_token`, `timezone`, `locale` are optional, and any additional custom attributes you include flow through to the user record. |
    | `event` | object | Free-form JSON, exposed to the campaign template as `{{event}}` (so `{{event.order_id}}` etc. resolve in Handlebars). May be `{}` if your template doesn't reference event data. |

    To send to multiple users, call the endpoint once per user (typically from your queue/worker layer).
  </Step>
  <Step title="Trigger the send">
    ```http
    POST /campaigns/{campaignId}/trigger HTTP/1.1
    Host: apix.spotzee.com
    Authorization: Bearer sk_…
    Idempotency-Key: launch-2026-04-28
    Spotzee-Version: 2026-04-28
    Content-Type: application/json

    {
      "user": {
        "external_id": "cust_001",
        "email": "ada@example.com",
        "first_name": "Ada"
      },
      "event": {
        "order_id": "12345",
        "total": 99.99
      }
    }
    ```

    On success the response is `{ "success": true }` and the send is enqueued for the standard delivery lifecycle.
  </Step>
  <Step title="Track delivery downstream">
    Once enqueued, a campaign send progresses through the standard delivery lifecycle. Watch progress on the campaign detail page in the Spotzee app, where send, delivery, and engagement counts update in real time. Outbound webhook delivery for per-message events is on the roadmap; today the in-app reporting view is the system of record.
  </Step>
</Steps>

## Pitfalls to avoid

<Warning>
  Don't reuse a campaign as a transactional template. Campaigns are designed for one-shot sends to a stable audience. For per-event transactional messages (welcome emails, password resets, receipt confirmations), trigger a journey instead. See [Trigger a journey](/guides/journey-trigger).
</Warning>

- **Don't trigger the same campaign twice in a row.** Spotzee will happily send twice. That's what the API is for. Use an `Idempotency-Key` so a network retry doesn't double-send. See [Idempotency](/guides/api-idempotency).
- **Don't expect templates to render with arbitrary data.** Template variables resolve against the user attributes (`{{user.…}}`) and the event payload (`{{event.…}}`). Anything not in either shows up as blank.
- **Don't trigger in a tight loop without batching.** Each call fires one send for one user; if you're sending to a large audience, queue and pace the calls (or use a `blast`-type campaign against a list instead).

## Reference

- API surface: see [Main API → Campaigns](/main-api/introduction)
- Safe retries: see [Idempotency](/guides/api-idempotency)
- Per-event automation instead: see [Trigger a journey](/guides/journey-trigger)
